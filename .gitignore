# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem
*.tmp

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# local data (bring your own) — raw statement + generated/state files, never committed
combined.csv
data/

# frozen Vite tool (legacy/) — deps + build output
legacy/node_modules/
legacy/dist/

# agent tooling — git worktrees + installed skills (reproducible from skills-lock.json)
# Exception: `autopilot` is a bespoke local skill (no upstream source / not in the
# lock), so its files are committed directly rather than reproduced from the lock.
.claude/worktrees/
.claude/skills/*
!.claude/skills/autopilot
.agents/skills/*
!.agents/skills/autopilot/
.vercel
