# Claude subscription OAuth token — generate with:  claude setup-token
# Used ONLY by `npm run classify` (server-side). Never exposed to the browser.
CLAUDE_CODE_OAUTH_TOKEN=

# Optional: a standard Anthropic API key instead of the OAuth token.
# If set, classify.ts prefers this. Pay-per-use.
# ANTHROPIC_API_KEY=
